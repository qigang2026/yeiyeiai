# [页面名称] - PRD

> **文档版本**: v1.0 · YYYY-MM-DD
> **作者**: [产品 / 设计 / 业务负责人]
> **文档状态**: 待评审 / 评审中 / 已定稿 / 实现中
> **关联 HTML 原型**: `[html-filename].html`
> **关联业务**: [业务线一句话说明]

---

## 0. 一句话定位

[用 1 句话说明这个页面/模块要解决什么问题、给谁用、达成什么业务结果。]

---

## 1. 业务背景与痛点

### 1.1 现状描述

[当前业务是如何运作的、用户在使用什么方案、上下游依赖是什么。]

### 1.2 痛点本质

1. **[痛点 A]**:用 1 句话说清楚"为什么现在的方案不行"。
2. **[痛点 B]**:
3. **[痛点 C]**:

### 1.3 价值主张 (Why now / Why this)

- **对用户**:
- **对业务**:
- **对公司战略**:

---

## 2. 角色模型与权限边界

### 2.1 涉及角色

| 角色 | 角色定义 | 在本模块中的典型动作 |
| --- | --- | --- |
| [角色 A] | [一句话定义] | [列举主要动作] |
| [角色 B] | | |

### 2.2 权限矩阵

| 能力 | 角色 A | 角色 B | 角色 C |
| --- | --- | --- | --- |
| [能力 1] | ✓ | ✗ | ✓ |
| [能力 2] | ✓ | ✓ | ✗ |

---

## 3. 信息架构与页面规划

### 3.1 页面入口 / 触发链路

```
[用户从哪个页面/场景进入] → [本页面] → [下一步动作]
```

### 3.2 页面分区（视觉布局）

```
┌─────────────────────────────────────────┐
│ [顶部区域：标题/状态/操作按钮]            │
├─────────────────────────────────────────┤
│ [主要内容区域]                            │
│  ├ [子区块 1]                            │
│  ├ [子区块 2]                            │
│  └ [子区块 3]                            │
├─────────────────────────────────────────┤
│ [底部区域：说明/链接]                     │
└─────────────────────────────────────────┘
```

### 3.3 关键交互清单

| 编号 | 触发元素 | 用户动作 | 预期反馈 |
| --- | --- | --- | --- |
| INT-001 | [按钮 / 链接] | [动作] | [反馈] |
| INT-002 | | | |

---

## 4. 核心功能模块设计

### 4.1 模块 A：[模块名]

#### 4.1.1 字段定义

| 字段名 | 类型 | 必填 | 校验规则 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `field_a` | string | ✓ | 长度 1-50 / 唯一 | — | |
| `field_b` | number | ✗ | 范围 1-100 | 1 | |

#### 4.1.2 业务规则

- **规则 1**：[用业务语言说明，不是代码]。
- **规则 2**：

#### 4.1.3 状态机（如适用）

```
[初始态] -- 触发条件 A --> [中间态] -- 触发条件 B --> [终态]
```

### 4.2 模块 B：[模块名]

[同上结构]

---

## 5. 交互细节与校验规则

### 5.1 表单校验

| 字段 | 触发时机 | 规则 | 错误提示文案 |
| --- | --- | --- | --- |
| [字段] | 用户离开输入框时 / 提交时 | [规则] | [文案] |

### 5.2 异常路径

| 异常场景 | 触发条件 | 用户感知 | 系统处理 |
| --- | --- | --- | --- |
| 网络断开 | 用户无网络 | 顶部黄色提示 | 表单内容暂存 |
| 服务异常 | 后端报错 | 顶部红色提示 | 不重试，等待用户重提交 |

### 5.3 可访问性

> 给有视力障碍用户使用的辅助功能。

- 键盘 Tab 顺序：[定义]
- 焦点指示器：[定义]
- 屏幕阅读器支持：[关键区域说明]

---

## 6. 异常与边界场景

### 6.1 数据边界

- **空数据**：[如何显示]
- **超长数据**：[如何截断/换行]
- **极端值**：[最大/最小值处理]

### 6.2 业务边界

- **跨租户/跨渠道**：[权限隔离]
- **重复提交**：[防止重复]
- **并发修改**：[最后写入生效，并提示用户]

---

## 7. 验收标准

> 上线前必须满足的条件，每条都打勾才算完成。

- [ ] **功能完整性**：[列出本页面所有功能点]
- [ ] **校验规则**：§5.1 中所有字段的校验均已实现
- [ ] **异常路径**：§6 中所有边界场景均有测试覆盖
- [ ] **性能**：首屏加载 < 1.5 秒，操作反馈 < 0.2 秒
- [ ] **浏览器兼容**：Chrome 100+ / Safari 15+ / Edge 100+
- [ ] **响应式**：≥ 1280px、1024px、768px 三个屏幕宽度下都验证
- [ ] **无障碍**：键盘可完全操作，焦点指示器可见
- [ ] **数据埋点**：[定义关键事件 PV/UV 与漏斗]
- [ ] **设计走查**：UI 同事 review 通过

---

## 8. 未来迭代 (本期不做)

| 版本 | 计划内容 | 价值评估 |
| --- | --- | --- |
| v2.0 | [内容] | [高/中/低] |
| v3.0 | [内容] | |

---

## 9. 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
| --- | --- | --- | --- |
| v1.0 | YYYY-MM-DD | [作者] | 初稿 |
| v1.1 | YYYY-MM-DD | [作者] | [修订内容] |

---

> **下一步**：评审通过后进入 UI 高保真 → 前端开发 → 测试 → 上线。
