# [页面名称] - PRD

> **文档版本**: v1.0 · YYYY-MM-DD
> **作者**: [产品 / 设计 / 业务负责人]
> **文档状态**: 待评审 / 评审中 / 已定稿 / 实现中
> **关联 HTML 原型**: `[html-filename].html`
> **关联后端服务**: [服务名 / 模块名]
> **关联业务**: [业务线一句话说明]

---

## 0. 一句话定位

[用 1 句话说明这个页面/模块要解决什么问题、给谁用、达成什么业务结果。]

---

## 1. 业务背景与痛点

### 1.1 现状描述

[当前业务是如何运作的、用户在使用什么方案、上下游依赖是什么。]

### 1.2 痛点本质

1. **[痛点 A]**:用 1 句话说清楚"为什么现在的方案不行"。
2. **[痛点 B]**:
3. **[痛点 C]**:

### 1.3 价值主张 (Why now / Why this)

- **对用户**:
- **对业务**:
- **对公司战略**:

---

## 2. 角色模型与权限边界

### 2.1 涉及角色

| 角色 | 角色定义 | 在本模块中的典型动作 |
| --- | --- | --- |
| [角色 A] | [一句话定义] | [列举主要动作] |
| [角色 B] | | |

### 2.2 权限矩阵

| 能力 | 角色 A | 角色 B | 角色 C |
| --- | --- | --- | --- |
| [能力 1] | ✓ | ✗ | ✓ |
| [能力 2] | ✓ | ✓ | ✗ |

---

## 3. 信息架构与页面规划

### 3.1 页面入口 / 触发链路

```
[用户从哪个页面/场景进入] → [本页面] → [下一步动作]
```

### 3.2 页面分区（视觉布局）

```
┌─────────────────────────────────────────┐
│ [顶部区域：标题/状态/操作按钮]            │
├─────────────────────────────────────────┤
│ [主要内容区域]                            │
│  ├ [子区块 1]                            │
│  ├ [子区块 2]                            │
│  └ [子区块 3]                            │
├─────────────────────────────────────────┤
│ [底部区域：说明/链接]                     │
└─────────────────────────────────────────┘
```

### 3.3 关键交互清单

| 编号 | 触发元素 | 用户动作 | 预期反馈 | 数据流 |
| --- | --- | --- | --- | --- |
| INT-001 | [按钮 / 链接] | [动作] | [反馈] | [写入/读取的数据] |
| INT-002 | | | | |

---

## 4. 核心功能模块设计

### 4.1 模块 A：[模块名]

#### 4.1.1 字段定义

| 字段名 | 类型 | 必填 | 校验规则 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `field_a` | string | ✓ | 长度 1-50 / 唯一 | — | |
| `field_b` | number | ✗ | 范围 1-100 | 1 | |

#### 4.1.2 业务规则

- **规则 1**：[用业务语言说明，不是代码]。
- **规则 2**：

#### 4.1.3 状态机（如适用）

```
[初始态] -- 触发条件 A --> [中间态] -- 触发条件 B --> [终态]
```

### 4.2 模块 B：[模块名]

[同上结构]

---

## 5. 数据契约

### 5.1 后端 API（如已确定）

| 接口 | 方法 | 路径 | 请求体 | 响应体 | 错误码 |
| --- | --- | --- | --- | --- | --- |
| [动作名] | POST | `/api/xxx` | `{...}` | `{...}` | 40001-40099 |

### 5.2 关键字段定义

```typescript
// 给出 TypeScript 接口或 JSON Schema，前端可以直接复用
interface TenantChannel {
  channel: 'none' | 'exiaoman' | 'logistics' | 'goods';
  referrer?: string;
}
```

### 5.3 状态机完整定义（如适用）

```
状态名 → 触发事件 → 下一状态
[草稿]    [提交]    [审核中]
[审核中]  [通过]    [已发布]
[审核中]  [驳回]    [已驳回]
```

---

## 6. 交互细节与校验规则

### 6.1 表单校验

| 字段 | 触发时机 | 规则 | 错误提示文案 |
| --- | --- | --- | --- |
| [字段] | 失焦/提交 | [规则] | [文案] |

### 6.2 异常路径

| 异常场景 | 触发条件 | 用户感知 | 系统处理 |
| --- | --- | --- | --- |
| 网络断开 | `navigator.onLine === false` | 顶部黄色 banner 提示 | 表单暂存到 localStorage |
| 接口 500 | 后端错误 | toast 报错 | 不重试，等待用户重提交 |

### 6.3 可访问性 (a11y)

- 键盘 Tab 顺序：[定义]
- 焦点指示器：[定义]
- ARIA 标签：[关键区域标注]

---

## 7. 异常与边界场景

### 7.1 数据边界

- **空数据**：[如何显示]
- **超长数据**：[如何截断/换行]
- **极端值**：[最大/最小值处理]

### 7.2 业务边界

- **跨租户/跨渠道**：[权限隔离]
- **重复提交**：[防抖 / 幂等]
- **并发修改**：[乐观锁 / 版本号]

---

## 8. 前端实现要点 (HTML → 组件映射)

> **给前端同事**：把 HTML 原型拆成 React/Vue 组件的指引。

### 8.1 组件拆分建议

| HTML 区块 | 推荐组件名 | 复用范围 | 备注 |
| --- | --- | --- | --- |
| 顶部状态卡 | `<ChannelStatCard>` | 全局 | |
| 主表格 | `<TenantTable>` | 本页 | |
| 新增弹窗 | `<TenantFormModal>` | 本页 | |

### 8.2 状态管理

- **本地状态**：[表单、弹窗开关]
- **服务端状态**：[列表、详情 — 推荐 SWR / React Query]
- **全局状态**：[当前租户、当前用户]

### 8.3 关键技术决策

| 决策点 | 推荐方案 | 理由 |
| --- | --- | --- |
| 表格性能（>1000 行） | 虚拟滚动 | 原型已用全量渲染 |
| 表单库 | React Hook Form + Zod | 校验规则可复用本 PRD §5 |
| 图表库 | ECharts | 后端已有 ECharts 数据结构 |

### 8.4 与现有代码的对接

- 复用组件：[列出已有的公共组件]
- 复用样式：[样式 token / Less 变量]
- 国际化：[文案 key 前缀]

---

## 9. 验收标准 / Definition of Done

- [ ] **功能完整性**：[列出本页面所有功能点]
- [ ] **校验规则**：§6.1 中所有字段的校验均已实现
- [ ] **异常路径**：§7 中所有边界场景均有测试覆盖
- [ ] **性能**：首屏 < 1.5s，操作反馈 < 200ms
- [ ] **浏览器兼容**：Chrome 100+ / Safari 15+ / Edge 100+
- [ ] **响应式**：≥ 1280px、1024px、768px 三个断点均验证
- [ ] **a11y**：键盘可完全操作，焦点指示器可见
- [ ] **埋点**：[定义关键事件 PV/UV 与漏斗]
- [ ] **设计走查**：UI 同事 review 通过

---

## 10. 未来迭代 (Out of Scope)

| 版本 | 计划内容 | 价值评估 |
| --- | --- | --- |
| v2.0 | [内容] | [高/中/低] |
| v3.0 | [内容] | |

---

## 11. 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
| --- | --- | --- | --- |
| v1.0 | YYYY-MM-DD | [作者] | 初稿 |
| v1.1 | YYYY-MM-DD | [作者] | [修订内容] |

---

> **下一步**：评审通过后进入 UI 高保真 → 前端开发 → 测试 → 上线。
